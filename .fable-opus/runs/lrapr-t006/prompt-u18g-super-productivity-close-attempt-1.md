Fable-Opus-Unit: lrapr-t006/u18g-super-productivity-close
Fable-Opus-Timeout-Minutes: 35

## Goal

Drive the super-productivity migrated lane's final six families in /Users/jacksm5pro/dev/open-source/versionless (commit `2dc30cf`; ledger `evidence/runs/angular-super-productivity-v2-13-15/u18f-capability-round.json`; 30 diagnostics; stage at `.versionless/stage/angular-super-productivity-v2-13-15-u18b/app`). Green, or a smaller itemized remainder, are both COMPLETED outcomes. Standing rulings: whole-tree diff for applicationFilesChanged; deterministic-modulo the recorded Sass-random files; logical-name parity.

Work the six families cheapest-first per the ledger (TS1259/TS2794 named cheapest; the chart.js options literal and the remaining TS2322/NG6002 census behind them). For each: existing capabilities where they fire; narrow surface/analyzer-provable transforms per the established idiom; refusals and itemization for what cannot be proven. The convergence history (269→110→57→33→30) says the tail is thinning — if this round reaches GREEN: build ×2 deterministic-modulo, logical-name parity vs the era lane, truthful final whole-tree applicationFilesChanged, the u18g record completing the cell's build story. If red: the smaller remainder with before/after counts.

Tests per idiom; overfitting guard green; whole repo gate green.

## File contract

- `packages/frameworks/angular/**`
- `packages/cli/src/fixture/**`
- `packages/cli/test/**`
- `packages/core/test/**`
- `evidence/runs/angular-super-productivity-v2-13-15/**`
- `fixtures/angular-super-productivity-v2-13-15/**`
- `package.json`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`

## Forbidden moves

- No packages/core/src changes, no packages/frameworks/react/**, packages/cli/src/witness/**, packages/trust/**, aggregate.json, evidence/trust/**, evidence/ingests/**, other evidence/runs/** dirs, scripts/**, docs/**.
- No app-name branches in product code; no fabricated evidence; truthful reds; no test weakening. Network only for consented reads (recorded). Strict TypeScript, magic-regexp, pathe, ufo. Do not commit or stage.

## Verification

```verify
pnpm exec tsc --noEmit
pnpm exec vp lint
pnpm exec vp test --project node
sh -c 'ls evidence/runs/angular-super-productivity-v2-13-15'
```

## Blocked permission

If a transform cannot stay generic (name the construct), a closed enumeration outside the contract surfaces, or the honest cut line exceeds this unit, return status "blocked" with specifics in open_questions instead of improvising.
