{
	"status": "blocked",
	"summary": "Calibrated the era lane against the live DOM with a new probe driver and recorded the measurements; the full AppSpec/journey/runner/tests exceed this unit. Measured: app boots into planning mode with zero tasks; five of six pinned style probes resolve; work-view-page resolves 0; real ngsw registers, activates and controls the page with 7 caches; font seam answered in-context; no failed requests; no first-route overflow; localStorage empty at boot. Two blockers: the host cannot read IndexedDB (localforage SUP/SUP_STORE), and the earned drag membership lives in witness-real-app.ts, outside the file contract. tsc clean, lint 0 errors.",
	"files_changed": ["packages/cli/src/fixture/angular-super-productivity-calibrate-run.ts"],
	"open_questions": [
		"WITNESS_REAL_APP_DRAG_SURFACES is defined in packages/core/src/receipts/witness-real-app.ts:75, which this packet's file contract excludes. The drag membership cannot be earned without adding 'angular-super-productivity' there and retiring the deliberate-absence comment at lines 55-62. Grant that file, or move the flip to a unit that owns it.",
		"Leg (e) reload-persistence cannot be measured: PlaywrightWitnessHost exposes only browserStorageKeys() (localStorage/sessionStorage) and the schema requires localforage IndexedDB keys under SUP/SUP_STORE. This needs a new IndexedDB key-reading capability in packages/cli/src/witness/playwright-host.ts plus a JourneyLifecycle member — sizeable work that should be its own unit or an explicit part of the re-cut.",
		"executeRun emits no real-service-worker record: the super-productivity schema requires run.serviceWorker with three phased checkpoints, outputFiles read before/after, and workerEvents, while the existing JourneyEvidence shapes only cover zero/refused/blocked workers. A new JourneyEvidence member and executeRun wiring are owed before any journey can satisfy the parser.",
		"Proposed re-cut of what is owed, in order: (1) host capabilities — IndexedDB key reader + real-ngsw checkpoint shape in executeRun; (2) AppSpec + journey legs (a)(c)(d) with the console/failed-request inventories pinned from a calibration pass on both lanes; (3) the drag leg plus the schema membership flip, with the witness-real-app.ts one-liner in contract; (4) the dedicated runner, mutation seam verified unique against dist-23, and tests."
	]
}
