Fable-Opus-Unit: lrapr-t006/u20a2-super-productivity-schema
Fable-Opus-Timeout-Minutes: 35

## Goal

Land the super-productivity witness core schema and wiring in /Users/jacksm5pro/dev/open-source/versionless — the u20a retry on corrected foundations (commit `2106cd8`). NO journeys, NO publish, NO drag-surface membership (earned in u20b by a measured drag).

The corrected bindings this schema uses:

- Era lane: bound via the u21 digest-correction record (`evidence/runs/angular-super-productivity-v2-13-15/u21-era-baseline-digest-correction.json` — raw-correct 64-file inventory) over the retained era dist.
- Migrated lane: bound via `u23-offline-font-lane.json` (62 artifacts, offline-proven, worker chunks, deterministic-modulo the ngsw timestamp with the Sass-random boundary-crossing fact recorded).
- Font seams: BOTH lanes now carry the era-faithful runtime Roboto link (u23 restored it) — one shared seam category, blocked at witness time per the mocked-seam idiom (the tiny-translator u22 shape).
- SW: ngsw in both lanes — real registrations under the non-masking discipline, telemetry checkpoints per lane, pinned at publish.
- Persistence: IndexedDB via localforage; storage keys recorded as measured.
- Accommodations: 19 steps / 8 decisions / 5 families + 1 payload (final as of u18h).

Deliver: the schema per the tiny-translator idiom (pinned source identity, bound build receipts with their determinism-modulo records, per-lane inventories, rendered-style probes, scroll, route shape, mutation slot, the lane supersede chain u18j→u23, parser/renderer/verifier/aggregate member, barrel export); wiring into `WITNESS_REAL_APP_NAMES` (framework 'angular'); tests per idiom; whole repo gate green.

## File contract

- `packages/cli/src/witness/**`
- `packages/cli/test/**`
- `packages/core/src/receipts/witness-angular-super-productivity.ts`
- `packages/core/src/receipts/witness-real-app.ts`
- `packages/core/src/index.ts`
- `packages/core/test/**`
- `fixtures/angular-super-productivity-v2-13-15/**`

## Forbidden moves

- No other packages/core changes; no packages/frameworks/**, packages/cli/src/fixture/**, packages/trust/**, aggregate.json, evidence/**, scripts/**, docs/**.
- No drag-surface membership; additive-only; no fabricated evidence; no app names in reusable surfaces beyond closed lists. No network. Strict TypeScript, magic-regexp, pathe, ufo. No test weakening. Do not commit or stage.

## Verification

```verify
pnpm exec tsc --noEmit
pnpm exec vp lint
pnpm exec vp test --project node
```

## Blocked permission

If the schema cannot express a bound fact, a binding target contradicts this packet (bring the reading), or the work exceeds this unit, return status "blocked" with specifics in open_questions instead of improvising.
