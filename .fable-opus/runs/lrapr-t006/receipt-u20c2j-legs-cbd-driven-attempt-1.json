{
	"status": "blocked",
	"summary": "Reordered the calibration apparatus to drive all mutations before the single reload (fixing the after-reload add-task-bar hang) and calibrated legs b/c/d against the live migrated-lane DOM (pageErrors 0). Findings force a block: (b) reorders ONLY via the up-drag (task2 handle-par onto nth-of-type(1)); the down/handle drop leaves order unchanged. (c) the pinned timeTracking rule is contradicted — ms-to-string hides sub-minute so .time-val is always '-' (no digit ever), and .play-icon-indicator does not render via the header start while the task .start-task-btn is hover-gated. (d) unmeasured but palette custom props resolve on body (measurable). Only in-scope write is the calibration file; tsc clean. Work exceeds one unit.",
	"files_changed": ["packages/cli/src/fixture/angular-super-productivity-calibrate-run.ts"],
	"open_questions": [
		"Leg (c): the pinned WitnessAngularSuperProductivityTimeTracking rule (playIndicatorPresent:true + trackedTimeValue-carries-a-digit) is contradicted by the live DOM — ms-to-string.pipe.ts omits seconds unless isShowSeconds is passed (the task template does not), so .time-val renders '-' for the whole practical duration of a journey, and .play-icon-indicator did not render via the reliable header-play start (the task's own .start-task-btn is hover-gated and unclickable). Approve the PM-flagged calibration-contradicted rule-pin revision: anchor leg (c) on the header icon flip play_arrow->pause plus current-task-set (proven by the pause icon), dropping the digit-time and play-indicator sub-rules? Or specify a different reliable time-tracking signal.",
		"Leg (b): confirm the calibrated gesture is the up-drag — drag task 2's `.drag-handle.handle-par` onto `task-list:first-of-type task:nth-of-type(1)` (settles ['task two','task']); the down-drag onto the second task's handle does not reorder. Pin this direction?",
		"Leg (d) is a substantial unmeasured sub-task (addProject dialog -> switch -> 2nd .config-section collapsible theme form -> input[type=color] key 'primary' -> .submit-button save -> before/after --palette-*-500 probe on body -> b/w shortcut). Re-cut it as its own unit (c+b land first), per the packet's stated cut?",
		"Baseline lane not yet run for b/c/d; only migrated measured. Re-cut must measure both lanes."
	]
}
