Fable-Opus-Unit: lrapr-t006/u18i-closure-correction-green
Fable-Opus-Timeout-Minutes: 35

## Goal

Close the super-productivity migrated lane's three bundler-level families and land GREEN in /Users/jacksm5pro/dev/open-source/versionless (commit `806e125`; ledger `evidence/runs/angular-super-productivity-v2-13-15/u18h-capability-round.json`; source census 0; stage at `.versionless/stage/angular-super-productivity-v2-13-15-u18b/app`).

PM rulings baked in:

1. **@ngx-formly closure correction AUTHORIZED**: 7.1.0 is Angular 18's line — the ecosystem entries for @ngx-formly/{core,material} correct to the newest 6.x line whose Angular peers the 16 cell satisfies, with the registry reading recorded AND a note on why the mechanical rule mis-selected (read 7.1.0's actual peer declarations — if its peers are loose/missing, that's the recorded cause and the rule may need a peer-strictness refinement; if the rule misread, fix the rule with a test). Consented re-install (VL-LEGACY-CORPUS-2026-08-10, URLs/digests recorded, offline after). Re-check the u18e unparameterised-base-class edit against the 6.x surface — if 6.x's FieldType differs, the capability re-run must handle it honestly.
2. **angular-material-css-vars `initMaterialCssVars` mixin**: read the installed 5.0.3's actual Sass surface first (the exports-map sass-condition machinery exists); capability if derivable (renamed/relocated mixin the surface proves), ruled accommodation if not.
3. **The JSON named-import** in `src/environments/environment.ts`: read the actual shape; if it's the resolveJsonModule named-vs-default interop, a narrow provable transform or a recorded accommodation — one line, whichever is honest.
4. GREEN close: build ×2 deterministic-modulo the recorded Sass-random files; logical-name parity vs the era lane; truthful whole-tree applicationFilesChanged separating capability edits from accommodations; the u18i record completing the cell's build story (accommodation inventory carried forward complete). If a NEW family surfaces, smaller-remainder rules apply.

Tests per idiom; overfitting guard green; whole repo gate green.

## File contract

- `packages/frameworks/angular/**`
- `packages/cli/src/fixture/**`
- `packages/cli/test/**`
- `packages/core/test/**`
- `evidence/runs/angular-super-productivity-v2-13-15/**`
- `fixtures/angular-super-productivity-v2-13-15/**`
- `package.json`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`

## Forbidden moves

- No packages/core/src changes, no packages/frameworks/react/**, packages/cli/src/witness/**, packages/trust/**, aggregate.json, evidence/trust/**, evidence/ingests/**, other evidence/runs/** dirs, scripts/**, docs/**.
- Accommodations never enter packages/frameworks; no app-name branches in product code; no fabricated evidence; truthful reds; no test weakening. Network only for the authorized consented install/reads (recorded). Strict TypeScript, magic-regexp, pathe, ufo. Do not commit or stage.

## Verification

```verify
pnpm exec tsc --noEmit
pnpm exec vp lint
pnpm exec vp test --project node
sh -c 'ls evidence/runs/angular-super-productivity-v2-13-15'
```

## Blocked permission

If the 6.x line cannot satisfy the cell (bring the peer readings), a new family exceeds the budget, or a closed enumeration outside the contract surfaces, return status "blocked" with specifics in open_questions instead of improvising.
