Fable-Opus-Unit: lrapr-t006/u18b-super-productivity-migrated-lane
Fable-Opus-Timeout-Minutes: 35

## Goal

Deliver the super-productivity MIGRATED lane in /Users/jacksm5pro/dev/open-source/versionless — the owed half of the Angular 8→16.2 cell (commit `d869d6c`: era lane recorded with its Sass-random() nondeterminism chased to the line; composed changeset at `evidence/runs/angular-super-productivity-v2-13-15/u18-composed-changeset.json`; 12 cell dispositions recorded).

PM rulings baked in:

1. **Parity oracle is the logical-name idiom** (u17d precedent): the era lane's Sass random() nondeterminism is a recorded application property — never seed/neutralize source on either lane. Nondeterministic files identified with their cause; stable files compared by bytes; witness arbitrates behavior later.
2. **chart.js 2→4: measure first.** Read what the app's chart usage actually is (ng2-charts components/directives, config shapes). A narrow, binding-resolved transform only for shapes the analyzer can prove; anything else is an itemized red demand, never a speculative major-bump rewrite.
3. **ng-pick-datetime: verify the fork lineage.** `@danielmoncada/angular-datetime-picker` is the community continuation of that exact package — verify the successor-fork provenance from registry/repo metadata (consented reads). If verifiable, aligning to it is a `successor-fork` cell disposition with the provenance recorded (API compatibility measured, not assumed — the import rewrite is generic if the fork keeps the module/symbol surface). If not verifiable or API-incompatible at the used surface, itemized red.

Deliver:

1. Apply the changeset to a stage tree; consented Angular 16.2 closure install (VL-LEGACY-CORPUS-2026-08-10, URLs/digests recorded, offline after).
2. Production build attempt(s) through the official builder; extend reusable capabilities as measured demands appear (ngrx 8→16 exercises createEffect's second app — record the cross-proof movement); RED with itemized demands is a completed outcome.
3. If green: build ×2, deterministic-modulo the recorded nondeterministic files, parity by logical name per ruling 1. Truthful `applicationFilesChanged` with digests.
4. Evidence under `evidence/runs/angular-super-productivity-v2-13-15/` (u18b-\* records); tests per idiom; overfitting guard green; whole repo gate green.

## File contract

- `packages/frameworks/angular/**`
- `packages/cli/src/fixture/**`
- `packages/cli/test/**`
- `packages/core/test/**`
- `evidence/runs/angular-super-productivity-v2-13-15/**`
- `fixtures/angular-super-productivity-v2-13-15/**`
- `package.json`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`

## Forbidden moves

- No packages/core/src changes, no packages/frameworks/react/**, packages/cli/src/witness/**, packages/trust/**, aggregate.json, evidence/trust/**, evidence/ingests/**, other evidence/runs/** dirs, scripts/**, docs/**.
- No app-name branches in product code; no fabricated evidence; truthful reds; no test weakening. Network only for consented installs/reads (recorded). Strict TypeScript, magic-regexp, pathe, ufo. Do not commit or stage.

## Verification

```verify
pnpm exec tsc --noEmit
pnpm exec vp lint
pnpm exec vp test --project node
sh -c 'ls evidence/runs/angular-super-productivity-v2-13-15'
```

## Blocked permission

If a demand cannot be generic (name the construct), the fork lineage cannot be verified and the red path is ambiguous, determinism-modulo fails beyond the recorded cause, or the honest cut line exceeds this unit, return status "blocked" with specifics in open_questions instead of improvising.
