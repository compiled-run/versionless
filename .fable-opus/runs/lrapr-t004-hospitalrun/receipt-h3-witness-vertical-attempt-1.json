{
	"status": "blocked",
	"summary": "Browser-probed both lanes before coding. The Vite 8 target is deterministically broken at runtime: pouchdb's `immediate` dependency uses unguarded `global` (queueMicrotask/MutationObserver/document), webpack 4 shimmed it and Vite does not, so the app throws ReferenceError and renders an empty #root in both build-vite-run1 and run2. No migrated journey, migrated 2/2, or mutation proof is achievable. The generic fix lives in packages/frameworks/react (forbidden) and would invalidate the digest-bound canonical build receipt. Separately, serviceWorkers:'block' makes the app's two register() calls emit 2 console errors per load, conflicting with clean()'s consoleErrors:0 and hardcoded cleanPage:true. Baseline vertical verified fully viable. No repo files written.",
	"files_changed": [],
	"open_questions": [
		"Who fixes the missing `global` shim for the CRA->Vite adapter? It is a generic create-react-app shape gap (define: { global: 'globalThis' } or a craEntryDocument/banner shim) in packages/frameworks/react, which my contract forbids. Should a prior unit land the adapter fix plus an offline dual-lane rebuild and a regenerated evidence/runs/react-hospitalrun/t004-run.json (current canonicalDigest 4f498c3cc7496af2d93a9be8a9aafec551178dfec150d8b34196abc64fc8d0ad), before this witness unit is re-cut?",
		"Under serviceWorkers:'block', HospitalRun's src/index.tsx emits exactly 2 console errors per page load from its own serviceWorker.register() rejecting. real-app-run.ts clean() asserts consoleErrors:0 and executeRun hardcodes cleanPage:true. Which is intended: (a) an app-scoped exact console-error inventory recorded in the receipt as part of the baseline-registers/target-does-not difference, with cleanPage redefined or a new field, or (b) a different non-masking mechanism that leaves consoleErrors at 0?",
		"Papercups recorded scroll as omitted-not-meaningful, but HospitalRun's /appointments surface genuinely scrolls (scrollHeight 1028 vs clientHeight 720 at 1280x720) while every other visited route is 720/720. Confirm the re-cut should exercise real scroll on the appointment schedule rather than reuse the papercups omission language."
	]
}
