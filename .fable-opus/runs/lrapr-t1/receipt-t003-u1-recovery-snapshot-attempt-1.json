{
	"status": "blocked",
	"summary": "Both preconditions could not be met: HEAD is correct (6223756) but refs/versionless/recovery/legacy-react-angular-production-readiness-pre-goal-20260810 already exists (81817169, 3-parent stash-shaped commit on base), and the contract manifest already exists with 38 entries in the exact schema verify-recovery-snapshot.ts expects. I probed the existing pair in a throwaway worktree: it passes this packet's verification (verified:true). However it is stale — 22 currently-dirty paths are uncaptured, including 8 tracked-modified evidence files, 6 .fable-opus state paths, 3 scripts/ files. Made no writes; tree and index unchanged.",
	"files_changed": [],
	"open_questions": [
		"The recovery ref and manifest already exist and already pass this packet's Verification block (verified:true, 38 entries). Should I (a) accept the existing snapshot as satisfying t003-u1 and report completed without rebuilding, or (b) rebuild a fresh snapshot capturing the 22 additional currently-dirty paths?",
		"If rebuilding: what is the exclusion policy for .fable-opus/? Forbidden-moves bars me from touching it, and it mutates during my own run, so including it makes the snapshot self-invalidating under the verifier's strict 'Unexpected restored paths' check. The existing 38-entry snapshot excludes it entirely, which is why it verifies.",
		"If rebuilding: should the 3 scripts/ files (including scripts/verify-recovery-snapshot.ts, the verifier itself) and the notes/ files t011-codex-run-audit.md and t012-tranche-spec.md be captured, given they postdate the original snapshot and the verifier must stay independent of what it verifies?",
		"Overwriting the existing ref would require a force update-ref that discards commit 81817169. Is destroying the prior recovery point sanctioned, or should a new dated/suffixed ref be used instead?"
	]
}
