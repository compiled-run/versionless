{
  "schemaVersion": "versionless.angular-eshop-webspa-source-migration.v1",
  "unit": "lrapr-t023/u5-frozen-adapter-migration",
  "consentId": "VL-LEGACY-CORPUS-2026-08-10",
  "cell": "angular-16-browser-builder",
  "appliedTo": {
    "tree": ".versionless/work/angular-eshop-webspa/target/app",
    "from": ".versionless/cache/angular-eshop-webspa-netcore2-2-source/corpus/eShopOnContainers-a387f21029f0b2d49614d165d5384717d2398f8e/src/Web/WebSPA",
    "commit": "a387f21029f0b2d49614d165d5384717d2398f8e",
    "excludedFromStage": [
      "node_modules",
      ".git"
    ],
    "excludedReason": "The corpus carries neither: it is the extracted release tarball, verified blob for blob against the pinned git tree by the ingest unit."
  },
  "applicationFilesScanned": 84,
  "applicationFilesChanged": 8,
  "workspaceFilesChanged": 3,
  "filesWritten": [
    {
      "path": "package.json",
      "kind": "workspace",
      "sha256After": "6a33834df819cdd3ef734584cc937d3dda50263927a0e95b10bc2ac52ffb9d7b",
      "changes": [
        "dependencies.@angular/animations: ^6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "dependencies.@angular/common: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "dependencies.@angular/compiler: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "dependencies.@angular/core: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "dependencies.@angular/forms: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "removed dependencies.@angular/http (was 6.1.4) — no successor line for angular-16-browser-builder: @angular/http stops at 7.2.16 and is deprecated on the registry with \"Package no longer supported. Use @angular/common instead\". Angular removed the package after the 7 line; there is no 8.x and no 16.x, so the `@angular/` family range this cell writes for its own packages names a version that was never published. The HTTP client that succeeded it is `@angular/common/http`, an entry point of a package this cell already carries, so the dependency is dropped rather than pinned to a v7 line beside a v16 framework.",
        "dependencies.@angular/platform-browser: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "dependencies.@angular/platform-browser-dynamic: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "dependencies.@angular/router: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "dependencies.@ng-bootstrap/ng-bootstrap: 3.1.0 -> ^15.1.2 — aligned to the community layer angular-16-browser-builder declares: @ng-bootstrap/ng-bootstrap versions one major *behind* the Angular major it supports, which is the whole hazard of reading this package by its name: the 16.x line is the Angular 17 line. Its peers discriminate exactly and they are what selects the line — every 15.x release (15.0.0 through 15.1.2) declares peer @angular/core, @angular/common, @angular/forms and @angular/localize \"^16.0.0\" with rxjs \"^6.5.3 || ^7.4.0\" and @popperjs/core \"^2.11.6\", where 14.2.0 and the rest of the 14.x line declare \"^15.0.0\" and 16.0.0 moves to \"^17.0.0\". 15.1.2 is therefore the newest line this cell accepts and the newest 15.x published; it declares dependency tslib ^2.3.0, exactly this cell's range, and no engines.node. The `latest` dist-tag points at 21.0.0, which declares peer @angular/core \"^22.0.0\" — a package this table read by its dist-tag alone would write a range six majors past this cell's linker. The compiled-with stamp was read anyway, because a peer is a hand-maintained claim: 15.1.2 carries version \"16.0.6\" in all 433 of its ɵɵngDeclare* partial declarations and ships the fesm2022 layout, where 16.0.0 carries \"17.0.0\" in 421 of them. Two further readings, because a peer this workspace does not itself declare is still a peer: @angular/localize ^16.0.0 is inside the ^16.2.0 family range this cell writes, and @popperjs/core ^2.11.6 is a different package name from the `popper.js` an era Bootstrap 4 workspace declares — npm supplies both as auto-installed peers, and nothing here rewrites a `popper.js` import into a @popperjs/core one. Read from https://registry.npmjs.org/@ng-bootstrap/ng-bootstrap, https://unpkg.com/@ng-bootstrap/ng-bootstrap@15.1.2/fesm2022/ng-bootstrap.mjs and https://unpkg.com/@ng-bootstrap/ng-bootstrap@16.0.0/fesm2022/ng-bootstrap.mjs under consent VL-LEGACY-CORPUS-2026-08-10 on 2026-08-14. The reading is a version fact and not a source claim: the 3.x line an Angular 6-era workspace pins published NgbModule with a `forRoot()` static and component selectors this line has since renamed, and nothing here rewrites a call site that names one.",
        "dependencies.ngx-toastr: ^9.0.2 -> ^17.0.2 — aligned to the community layer angular-16-browser-builder declares: ngx-toastr is the second entry the peer-strictness refinement decides, and its peers fail to discriminate in the plainer of the two ways: they are identical across four consecutive majors. 20.0.5, the newest release and the `latest` dist-tag, declares peer @angular/common and @angular/core \"^21.0.0\" and rxjs \"^7.8.2\"; the ^16.2.0 this cell writes excludes it. Every release from 17.0.0 through 19.1.0 declares exactly {\"@angular/core\": \">=16.0.0-0\", \"@angular/common\": \">=16.0.0-0\", \"@angular/platform-browser\": \">=16.0.0-0\"}, no rxjs peer and no engines.node, so on the peer rule alone the newest satisfying line is 19.1.0 — and nothing in those declarations separates a release built for Angular 16 from one built for Angular 18. The compiled-with stamps separate them: 19.1.0 and 19.0.0 carry Angular \"18.0.0\" in their partial declarations, 18.0.0 carries \"17.0.3\", and 17.0.2, 17.0.1 and 17.0.0 all carry \"16.0.1\". 17.0.2 is therefore the newest line this cell links, and it is the newest 17.x published. Its only dependency is tslib ^2.3.0, exactly the range this cell writes. One further reading, because a workspace configuration can name a package's files directly rather than importing them: 17.0.2 still publishes toastr.css at the package root (https://unpkg.com/ngx-toastr@17.0.2/toastr.css), the same path the Angular 8-era 10.0.4 published, so an `angular.json` styles entry naming ./node_modules/ngx-toastr/toastr.css keeps resolving across this hop. Read from https://registry.npmjs.org/ngx-toastr and https://unpkg.com/ngx-toastr@17.0.2/fesm2022/ngx-toastr.mjs under consent VL-LEGACY-CORPUS-2026-08-10 on 2026-08-14. The era 10.0.4 line declares peer @angular/core, @angular/common and @angular/platform-browser \">=6.0.0 <9.0.0\" and rxjs \"^6.1.0\" — two independent collisions with this cell, either of them fatal to the tree, and both answered by the same move.",
        "removed dependencies.preboot (was 6.0.0-beta.5) — no successor line for angular-16-browser-builder: preboot published ninety-four versions and the newest of them is 8.0.0, published 2021-01-18; it is the `latest` dist-tag and the only tag the package carries. Its declared peer does not exclude it — 8.0.0 declares @angular/common and @angular/core \">=11.0.0\" and dependency tslib ^2.0.0, both satisfied by this cell — so a reading that stopped at the resolver would align the package and be wrong, for the same reason jw-bootstrap-switch-ng2 was wrong: what 8.0.0 published is a pre-Ivy ViewEngine library. Its tarball ships preboot.metadata.json beside bundles/preboot.umd.js, esm2015/ and fesm2015/; the fesm bundle declares its module the ViewEngine way (`PrebootModule.decorators = [{ type: NgModule, args: [...] }]`) and carries no ɵɵngDeclare* partial declaration and no ɵɵdefineNgModule at all, and module.d.ts publishes `export declare class PrebootModule` with no ɵmod, ɵfac or ɵinj. The converter that used to make such a library consumable is gone: @angular/compiler-cli 16.2.12 ships `ngcc` only as a stub whose own message reads \"As of Angular 16, 'ngcc' is no longer required and not invoked during CLI builds\". The registry also marks every version of the package deprecated with \"This package is no longer maintained and is unnecessary with the recent versions of Angular.\" There is therefore no line to align to: not a newer one, because none exists, and not the newest one, because this cell's linker does not consume what it publishes. The package is dropped as a declared difference. Doing so turns any `PrebootModule` or `EventReplayer` import into a source demand the compiler states by name; an application that declares preboot and never imports it loses a declaration and nothing else, and choosing a replacement server-side-render replay library is a source decision this table does not make. Read from https://registry.npmjs.org/preboot and from the published tarball https://registry.npmjs.org/preboot/-/preboot-8.0.0.tgz under consent VL-LEGACY-CORPUS-2026-08-10 on 2026-08-14.",
        "dependencies.rxjs: 6.2.2 -> ~7.8.0 — aligned to angular-16-browser-builder",
        "dependencies.zone.js: ^0.8.26 -> ~0.13.0 — aligned to angular-16-browser-builder",
        "devDependencies.@angular-devkit/build-angular: ~0.7.0 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "devDependencies.@angular/cli: ^6.1.5 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "devDependencies.@angular/compiler-cli: 6.1.4 -> ^16.2.0 — aligned to angular-16-browser-builder",
        "devDependencies.@types/jasmine: 2.8.8 -> ~4.3.0 — aligned to the test toolchain angular-16-browser-builder generates",
        "removed devDependencies.codelyzer (was ~4.4.4) — released by a builder target the workspace migration removed",
        "removed devDependencies.tslint (was ~5.11.0) — released by a builder target the workspace migration removed",
        "devDependencies.typescript: 2.9.2 -> ~5.1.3 — aligned to angular-16-browser-builder",
        "scripts.build:prod: --prod -> --configuration production — `--prod` was a shorthand for `--configuration production` and was removed from the Angular CLI after the 11 line; a 12-or-later CLI reports it as an unknown argument and exits before the builder runs. The replacement selects the same configuration by name, which is why the retarget is refused where the migrated workspace does not declare one.",
        "removed --extract-css from scripts.build:prod — --extract-css sets a builder option this migration removed from the workspace at projects.WebSPA.architect.build.configurations.production.extractCss; the @angular-devkit/build-angular:browser of angular-16-browser-builder rejects the option, so the script that passes it would be refused by the builder schema before the build began."
      ]
    },
    {
      "path": "angular.json",
      "kind": "workspace",
      "sha256After": "2278fdddc6d5e4afa39540458cffe3f5f35bc5da26106d9c2b78120f1b821d3f",
      "changes": [
        "removed defaultProject (was \"WebSPA\")",
        "projects.WebSPA.architect.build.options.polyfills: \"Client/polyfills.ts\" -> [\"Client/polyfills.ts\"]",
        "added projects.WebSPA.architect.build.options.optimization = {\"scripts\":true,\"styles\":true,\"fonts\":{\"inline\":false}}",
        "projects.WebSPA.architect.build.configurations.production.optimization: true -> {\"scripts\":true,\"styles\":true,\"fonts\":{\"inline\":false}}",
        "removed projects.WebSPA.architect.build.configurations.production.extractCss (was true)",
        "projects.WebSPA.architect.test.options.polyfills: \"Client/polyfills.ts\" -> [\"Client/polyfills.ts\"]",
        "removed projects.WebSPA.architect.lint (was @angular-devkit/build-angular:tslint)",
        "removed projects.WebSPA-e2e.architect.e2e (was @angular-devkit/build-angular:protractor)",
        "removed projects.WebSPA-e2e.architect.lint (was @angular-devkit/build-angular:tslint)"
      ]
    },
    {
      "path": "tsconfig.json",
      "kind": "workspace",
      "sha256After": "15703e0f06d1453809d8c2d92f2be86087bd315ea392f8c17b8c2a6acdcba97e",
      "changes": [
        "compilerOptions.target: \"es5\" -> \"ES2022\"",
        "compilerOptions.module: \"es2015\" -> \"ES2022\"",
        "added compilerOptions.useDefineForClassFields = false"
      ]
    },
    {
      "path": "Client/modules/basket/basket.module.ts",
      "kind": "application",
      "sha256After": "32f7deb77101f952cc79c47e0d94c57c495c09e80a9c74b35b9bf27da1989436",
      "changes": [
        "line 17: module-with-providers-type-argument <BasketModule> read from enclosing-class"
      ]
    },
    {
      "path": "Client/modules/basket/basket.service.ts",
      "kind": "application",
      "sha256After": "f74d8bea41a790a41c137a2e5781704ad9fb637ca45bfb67ed99446d57d8d036",
      "changes": [
        "line 27: subject-void-type-argument <void> on basketDropedSource, proved by 1 zero-argument next call(s)"
      ]
    },
    {
      "path": "Client/modules/shared/services/basket.wrapper.service.ts",
      "kind": "application",
      "sha256After": "d60be44fd6fd628eb276943fbb28f16c3dc5bd9c72843ab35d40ad50181b7088",
      "changes": [
        "line 20: subject-void-type-argument <void> on orderCreatedSource, proved by 1 zero-argument next call(s)"
      ]
    },
    {
      "path": "Client/modules/shared/services/configuration.service.ts",
      "kind": "application",
      "sha256After": "660fb02a3aca20f0d09e45090e6e906cf4c3a4f616ee010c7e8f6c37e4cc619c",
      "changes": [
        "line 12: subject-void-type-argument <void> on settingsLoadedSource, proved by 1 zero-argument next call(s)"
      ]
    },
    {
      "path": "Client/modules/shared/services/signalr.service.ts",
      "kind": "application",
      "sha256After": "5dd78ca610790922478e045c24c6f5d53aaa0a8e5ac444a34dcb8a464fb6a8c7",
      "changes": [
        "line 12: subject-void-type-argument <void> on msgSignalrSource, proved by 1 zero-argument next call(s)"
      ]
    },
    {
      "path": "Client/modules/shared/shared.module.ts",
      "kind": "application",
      "sha256After": "901c1b67cd650c1077b08ec2e078edeed44803a2abc8aab6f7dd658ffa2ce0fd",
      "changes": [
        "line 60: module-with-providers-type-argument <SharedModule> read from enclosing-class"
      ]
    },
    {
      "path": "Client/polyfills.ts",
      "kind": "application",
      "sha256After": "4d2b52b763684146c98966177defbb452127fd434c50e637850827228048e0e2",
      "changes": [
        "line 56: module-specifier zone.js/dist/zone -> zone.js"
      ]
    },
    {
      "path": "Client/test.ts",
      "kind": "application",
      "sha256After": "b400020022812a7490a14879be151eb19335c856af0841beee634819f7c5ccb9",
      "changes": [
        "line 3: module-specifier zone.js/dist/long-stack-trace-zone -> zone.js/plugins/long-stack-trace-zone"
      ]
    }
  ],
  "filesRemoved": [
    "tslint.json",
    "package-lock.json"
  ],
  "declaredDifferences": [
    "projects.WebSPA.architect.build.options.optimization builds with font inlining disabled (`optimization.fonts.inline: false`). On the 16.2 line that option defaults to on, and it makes the build itself fetch every Google Fonts and Adobe Fonts stylesheet the application links and paste the answer into the emitted index.html. The era build did no such thing: it emitted the application's own link element and the browser fetched the stylesheet at runtime. This cell keeps that behaviour — the migrated document ships the same link and the same runtime fetch — rather than moving a third-party request into the build, where it would make the emitted bytes depend on what a font host served on the build day and on whether the build host could reach it at all. Inlining would not have removed the runtime request in any case: the inlined stylesheet still points at the font host for the font files themselves.",
    "projects.WebSPA.architect.build.configurations.production.optimization builds with font inlining disabled (`optimization.fonts.inline: false`). On the 16.2 line that option defaults to on, and it makes the build itself fetch every Google Fonts and Adobe Fonts stylesheet the application links and paste the answer into the emitted index.html. The era build did no such thing: it emitted the application's own link element and the browser fetched the stylesheet at runtime. This cell keeps that behaviour — the migrated document ships the same link and the same runtime fetch — rather than moving a third-party request into the build, where it would make the emitted bytes depend on what a font host served on the build day and on whether the build host could reach it at all. Inlining would not have removed the runtime request in any case: the inlined stylesheet still points at the font host for the font files themselves.",
    "projects.WebSPA.architect.lint ran @angular-devkit/build-angular:tslint, and angular-16-browser-builder carries no TSLint line. The target was removed as a declared migration difference: the era workspace lints with TSLint and the migrated workspace does not, and no replacement lint capability was chosen here.",
    "projects.WebSPA-e2e.architect.lint ran @angular-devkit/build-angular:tslint, and angular-16-browser-builder carries no TSLint line. The target was removed as a declared migration difference: the era workspace lints with TSLint and the migrated workspace does not, and no replacement lint capability was chosen here.",
    "tslint.json configures TSLint, and angular-16-browser-builder carries no TSLint line for it to configure. The file was removed with the targets that read it; no rule in it was translated to another linter.",
    "dependencies.@angular/http was removed: the migrated workspace no longer carries it. @angular/http stops at 7.2.16 and is deprecated on the registry with \"Package no longer supported. Use @angular/common instead\". Angular removed the package after the 7 line; there is no 8.x and no 16.x, so the `@angular/` family range this cell writes for its own packages names a version that was never published. The HTTP client that succeeded it is `@angular/common/http`, an entry point of a package this cell already carries, so the dependency is dropped rather than pinned to a v7 line beside a v16 framework.",
    "dependencies.preboot was removed: the migrated workspace no longer carries it. preboot published ninety-four versions and the newest of them is 8.0.0, published 2021-01-18; it is the `latest` dist-tag and the only tag the package carries. Its declared peer does not exclude it — 8.0.0 declares @angular/common and @angular/core \">=11.0.0\" and dependency tslib ^2.0.0, both satisfied by this cell — so a reading that stopped at the resolver would align the package and be wrong, for the same reason jw-bootstrap-switch-ng2 was wrong: what 8.0.0 published is a pre-Ivy ViewEngine library. Its tarball ships preboot.metadata.json beside bundles/preboot.umd.js, esm2015/ and fesm2015/; the fesm bundle declares its module the ViewEngine way (`PrebootModule.decorators = [{ type: NgModule, args: [...] }]`) and carries no ɵɵngDeclare* partial declaration and no ɵɵdefineNgModule at all, and module.d.ts publishes `export declare class PrebootModule` with no ɵmod, ɵfac or ɵinj. The converter that used to make such a library consumable is gone: @angular/compiler-cli 16.2.12 ships `ngcc` only as a stub whose own message reads \"As of Angular 16, 'ngcc' is no longer required and not invoked during CLI builds\". The registry also marks every version of the package deprecated with \"This package is no longer maintained and is unnecessary with the recent versions of Angular.\" There is therefore no line to align to: not a newer one, because none exists, and not the newest one, because this cell's linker does not consume what it publishes. The package is dropped as a declared difference. Doing so turns any `PrebootModule` or `EventReplayer` import into a source demand the compiler states by name; an application that declares preboot and never imports it loses a declaration and nothing else, and choosing a replacement server-side-render replay library is a source decision this table does not make. Read from https://registry.npmjs.org/preboot and from the published tarball https://registry.npmjs.org/preboot/-/preboot-8.0.0.tgz under consent VL-LEGACY-CORPUS-2026-08-10 on 2026-08-14.",
    "devDependencies.codelyzer was removed: the migrated workspace no longer carries it. codelyzer stops at 6.0.2, a TSLint rule set for Angular 9-era workspaces. Nothing succeeds it on the TSLint line and its ESLint replacement is @angular-eslint, which this cell already aligns as a family.",
    "devDependencies.tslint was removed: the migrated workspace no longer carries it. TSLint stops at 6.1.3 and was deprecated by its own maintainers in favour of ESLint before Angular 13 shipped. There is no TSLint release for any Angular line at or beyond this cell, so the whole TSLint toolchain is dropped rather than carried.",
    "package-lock.json was removed: it is the era closure's own resolution, and the migrated manifest no longer asks for that closure. @angular-devkit/build-angular is locked at 0.7.5 and now declared ^16.2.0; @angular/animations is locked at 6.1.4 and now declared ^16.2.0; @angular/cli is locked at 6.1.5 and now declared ^16.2.0 and 13 further declaration(s). A lockfile is read before a manifest, so leaving it in the tree makes the package manager resolve the era pins it names and refuse the install over versions the migrated manifest never declared. The migrated manifest is to be installed lock-free and re-locked by that install; the era resolution is a fact about the era workspace and is not carried into this one, so the reproducibility it provided is a declared loss rather than a preserved property."
  ],
  "unhandled": [
    "projects.WebSPA-e2e.architect.e2e used @angular-devkit/build-angular:protractor, which the 16.2 devkit does not ship; the target was removed and no replacement capability was chosen for it",
    "dependencies.@aspnet/signalr is declared at 1.0.3 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.bootstrap is declared at 4.1.3 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.core-js is declared at ^2.5.7 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.file-loader is declared at 2.0.0 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.font-awesome is declared at 4.7.0 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.isomorphic-fetch is declared at 2.2.1 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.jquery is declared at ^3.3.1 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.normalize.css is declared at 8.0.0 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.popper.js is declared at ^1.14.4 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "dependencies.webpack is declared at ^4.17.1 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.@types/core-js is declared at 2.5.0 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.@types/hammerjs is declared at 2.0.35 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.@types/node is declared at ~10.9.2 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.@types/protractor is declared at 4.0.0 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.@types/selenium-webdriver is declared at 3.0.10 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.npm-watch is declared at 0.5.0 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.sass-lint is declared at 1.12.1 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.ts-helpers is declared at 1.1.2 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.ts-node is declared at ~7.0.1 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.typedoc is declared at 0.12.0 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "devDependencies.url-loader is declared at 1.1.1 and angular-16-browser-builder has read no line for it: it is named by none of the cell's exact ranges, its generated test toolchain or its community layer, and no family prefix it writes matches the name. The era declaration was carried into the migrated manifest unchanged, which is the only honest edit for a package nobody read — and it is reported here rather than left silent, because an era pin standing beside an aligned framework is where a dependency-resolution refusal comes from, and a lane is owed that before the package manager tells it.",
    "@angular/http is named by this application's own source — Client/modules/basket/basket.service.ts:2 (module-import `@angular/http`), Client/modules/campaigns/campaigns.service.ts:2 (module-import `@angular/http`), Client/modules/catalog/catalog.service.ts:2 (module-import `@angular/http`), Client/modules/orders/orders.service.ts:2 (module-import `@angular/http`), Client/modules/shared/services/security.service.ts:2 (module-import `@angular/http`), Client/modules/shared/services/security.service.ts:2 (module-import `@angular/http`), Client/modules/shared/services/security.service.ts:2 (module-import `@angular/http`), Client/modules/shared/shared.module.ts:5 (module-import `@angular/http`), Client/modules/shared/shared.module.ts:5 (module-import `@angular/http`) — and is not declared by the migrated manifest, because angular-16-browser-builder read the package and found no line of it to declare: @angular/http stops at 7.2.16 and is deprecated on the registry with \"Package no longer supported. Use @angular/common instead\". Angular removed the package after the 7 line; there is no 8.x and no 16.x, so the `@angular/` family range this cell writes for its own packages names a version that was never published. The HTTP client that succeeded it is `@angular/common/http`, an entry point of a package this cell already carries, so the dependency is dropped rather than pinned to a v7 line beside a v16 framework. The import site is the application's, so nothing here can close it: the source asks for a package this cell has no successor for.",
    "compilerOptions.lib was left as declared; the 16.2 line does not require a specific lib set and narrowing one could remove a type surface the application relies on"
  ],
  "notEstablished": [
    "Writing a changeset into a tree is not a build. Nothing here establishes that the applied tree installs, compiles or emits anything; the lane record beside this one states what happened when that was attempted.",
    "No application source was edited by hand in this lane. Every byte that differs from the corpus was written by a frozen adapter transform, and each is itemised in the changeset record by file and by change."
  ],
  "digest": "a55bf1476323a4d79b5a42a1676c820ee672bd97b81b0a84812294a817d21ef9"
}
